<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.activity_four"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="10" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- MAIN ACTIVITY -->
        <activity
            android:name=".Activity_Four"
            android:label="@string/title_activity_activity__four" >
        </activity>

        <!-- COOKING MAMA ACTIVITY -->
        <activity
            android:name=".CookingMamaActivity"
            android:label="@string/title_activity_cooking_mama" >
        </activity>

        <!-- ANGRY BIRDS -->
        <activity
            android:name=".AngryBirdsActivity"
            android:label="@string/title_activity_angry_birds" >
        </activity>

        <!-- PLANTS VS ZOMBIES -->
        <activity
            android:name=".PlantsActivity"
            android:label="@string/title_activity_plants" >
        </activity>
        <activity
            android:name=".Tab1"
            android:label="@string/title_activity_tab1" >
        </activity>
        <activity
            android:name=".Tab2"
            android:label="@string/title_activity_tab2" >
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>